services:
  - type: web
    name: motivational-dialogue-api
    env: python
    region: singapore   # or "oregon", "frankfurt", etc.
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port 10000
    plan: free
